import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#fff', // '#003460',
    height: 60,
    justifyContent: 'center',
    paddingHorizontal: 15,
  },
});
